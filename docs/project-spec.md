# 🌱 FarmBalance — 통합 기능명세서 (기획안 + 기능명세서 취합)

> **작성일**: 2026-04-22  
> **기반**: 수경·지윤·지은·현석 기획안 4개 + 기능명세서 4개 (총 8개 문서)

---

## 1. 제품 개요

| 항목          | 내용                                                               |
| ------------- | ------------------------------------------------------------------ |
| **제품명**    | FarmBalance (팜밸런스)                                             |
| **슬로건**    | "작은 땅에서 시작하는 큰 미래, 데이터로 균형을 맞추는 스마트 파밍" |
| **대상 지역** | 경기도 양평군 (시범) → 전국 확대                                   |
| **핵심 가치** | 데이터 기반 작물 수급 예측 → 농가 소득 안정화 + 지역 농업 상생     |

---

## 2. 도메인 · 역할 체계

### 2.1 도메인 구조 (3개)

| 도메인            | 설명                                   | 포함 모듈                                                                  |
| ----------------- | -------------------------------------- | -------------------------------------------------------------------------- |
| **유저 도메인**   | 일반유저와 농부가 사용하는 서비스 영역 | 회원관리, 농장관리, 종자등록, 작물밸런스, AI추천, 상점, 커뮤니티, 가게정보 |
| **관리자 도메인** | 플랫폼 전체 운영·관리 영역             | 유저관리, RAG데이터관리, 가게관리, 커뮤니티관리, 상점관리, 관리자대시보드  |
| **지자체 도메인** | 지역 농업 현황 모니터링·정책 지원 영역 | 수급대시보드, 보고서, 정책알림                                             |

### 2.2 역할자 정의 (4개)

```
┌─────────────────── 유저 도메인 ───────────────────┐
│                                                   │
│  [일반유저] ──농장 등록 신청──→ [농부]              │
│      │                           │                │
│      └─ 커뮤니티,                 └─ + 종자 등록,  │
│         상점(구매/판매),              밸런스 조회,  │
│         정책 매칭                     AI 추천,      │
│                                      가게 정보     │
└───────────────────────────────────────────────────┘

┌─── 관리자 도메인 ───┐    ┌─── 지자체 도메인 ───┐
│ [관리자]            │    │ [지자체]            │
│  전체 시스템 관리   │    │  대시보드(조회)     │
│  데이터·유저·상점   │    │  수급 보고서        │
│  RAG 관리           │    │  정책 알림 발송     │
└─────────────────────┘    └─────────────────────┘
```

### 2.3 역할별 접근 권한 매트릭스

| 기능                  | 일반유저 | 농부  | 관리자  | 지자체  |
| --------------------- | :------: | :---: | :-----: | :-----: |
| 회원가입/로그인       |    ✅     |   ✅   |    ✅    |    ✅    |
| 커뮤니티 (읽기/쓰기)  |    ✅     |   ✅   | ✅(관리) |    ❌    |
| 상점 (구매)           |    ✅     |   ✅   |    ❌    |    ❌    |
| 상점 (판매)           |    ✅     |   ✅   | ✅(관리) |    ❌    |
| 농장 등록 신청        |    ✅     |   —   | ✅(승인) |    ❌    |
| 농장/토지 관리        |    ✅     |   ✅   | ✅(조회) |    ❌    |
| 종자 등록             |    ✅     |   ✅   | ✅(조회) |    ❌    |
| 작물 밸런스 조회      |    ✅     |   ✅   |    ✅    |    ✅    |
| AI 작물 추천          |    ✅     |   ✅   |    ✅    |    ❌    |
| 정책 매칭 / 공문 생성 |    ✅     |   ✅   | ✅(관리) |    ❌    |
| 가게 정보 조회        |    ✅     |   ✅   | ✅(관리) |    ❌    |
| 수급 대시보드         |    ❌     |   ❌   | ✅(전체) | ✅(조회) |
| 유저 관리             |    ❌     |   ❌   |    ✅    |    ❌    |
| RAG 데이터 관리       |    ❌     |   ❌   |    ✅    |    ❌    |

---

## 3. 통합 기능 목록 (38개)

### 3.1 공통 기능 (4개)

| ID      | 기능명          | 설명                                                          |
| ------- | --------------- | ------------------------------------------------------------- |
| COM-001 | 회원가입        | SMS/이메일 인증, 역할별 추가 필드, 소셜 로그인(카카오)        |
| COM-002 | 로그인/로그아웃 | JWT + Refresh Token, BFF httpOnly 쿠키, 5회 실패 시 30분 잠금 |
| COM-003 | 마이페이지      | 프로필 관리 (완성도 %), 토지정보, 정책 매칭 이력              |
| COM-004 | 알림 시스템     | 별도 모듈, 쏠림 경고·권고·주문 알림 등                        |

### 3.2 유저 기능 (15개)

| ID      | 기능명                   | 설명                                                                                                                        |
| ------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------- |
| USR-001 | 농업인 등록 신청         | 일반유저 → 농장 등록(토지증명서 첨부) → 관리자 검토·승인 → 농부 역할 부여                                                   |
| FRM-001 | 농장 등록 및 관리        | 이름, 주소, 면적, 토양유형, 토지증명서 첨부, 승인 플로우 (PENDING→APPROVED)                                                 |
| FRM-002 | 종자 구매 등록           | 종자명(씨앗/종자/모종)·수량·구매처·(영수증 사진 인증), 등록 완료 시 → AI 맞춤 정책 매칭 + 예측 리포트 즉시 제공 (등록 유도) |
| FRM-003 | 파종 계획 등록           | 작물·면적·파종일·목표수확량, 농장 면적 초과 검증                                                                            |
| FRM-004 | 수확 실적 등록           | 실제 수확량·수확일·품질등급                                                                                                 |
| FRM-005 | 작물 밸런스 조회         | 게이지 차트 + 5단계 수급 상태 + 시계열 추이 + 필터(지역/작물/연도)                                                          |
| FRM-006 | AI 작물 추천             | Gemini API, 점수 산정(수급35%+환경25%+수익25%+역량15%), Top5 + 근거 표시                                                    |
| FRM-007 | 가게 정보 조회           | 카카오맵 연동, 초보 친화도 리뷰, 제휴 가게 상단 노출                                                                        |
| USR-002 | 상점 구매                | 상품 탐색·장바구니·주문·결제                                                                                                |
| USR-004 | 상품 등록/관리 (판매)    | 사진 한 장 간편 등록, 가격·재고 관리 (일반유저·농부 모두 가능)                                                              |
| USR-005 | 주문 접수/관리 (셀러)    | 주문 상태 관리 (접수→발송→완료) (일반유저·농부 모두 가능)                                                                   |
| USR-003 | 커뮤니티                 | 카테고리별 게시판(자유/정보공유/Q&A), 답변 채택 시스템, 댓글                                                                |
| FRM-010 | 내 등록 이력 조회        | 종자·파종·수확 이력, 상태/연도 필터                                                                                         |
| FRM-011 | 재배 의향 수정/취소      | delta 반영, 논리 삭제                                                                                                       |
| FRM-012 | 정책 매칭 / AI 공문 생성 | 재배 희망 작물 기반 지자체 지원 정책 자동 매칭, AI 신청 공문 초안 생성 (PDF)                                                |

### 3.3 지자체 기능 (6개)

| ID      | 기능명               | 설명                                                        |
| ------- | -------------------- | ----------------------------------------------------------- |
| GOV-001 | 수급 대시보드        | 경고배지, Top10 작물, 등록률, YoY 비교, 쏠림 감지 자동 경고 |
| GOV-002 | 인접 지역 비교 분석  | 전국 평균 비교, 정책 제언                                   |
| GOV-003 | 재배 의향 현황       | 3가지 뷰 (작물별/지역별/시계열)                             |
| GOV-004 | 보고서 생성          | PDF 비동기 생성                                             |
| GOV-005 | 농가 권고/공지 발송  | 다채널 타겟팅, 일괄 전송                                    |
| GOV-006 | 수급 데이터 다운로드 | XLSX/CSV 내보내기                                           |

### 3.4 관리자 기능 (12개)

| ID      | 기능명                | 설명                                                           |
| ------- | --------------------- | -------------------------------------------------------------- |
| ADM-001 | 사용자 관리           | 목록·역할변경·정지·재활성화 + 감사 로그                        |
| ADM-002 | 농부 승인/반려        | 토지증명서 검토 후 농장 등록 승인/반려, 승인 시 농부 역할 부여 |
| ADM-003 | 작물 마스터 관리      | 등록/수정/비활성화, 밸런스 재계산 트리거                       |
| ADM-004 | 외부 데이터 연동 관리 | API 수집 설정, 갱신 주기, 이상값 탐지                          |
| ADM-005 | 밸런스 엔진 관리      | 임계치·가중치·주기 조정, Hot Reload                            |
| ADM-006 | 시스템 헬스 모니터링  | 구성요소별 상태 + 메트릭, 히트맵 + API 모니터링                |
| ADM-007 | 데이터 정합성 검증    | 면적 검증/동기화 확인/고아 데이터 탐지                         |
| ADM-008 | 커뮤니티 관리         | 삭제, 공지, 신고 처리                                          |
| ADM-009 | 상점 관리             | 상품 승인/반려/비활성화, 분쟁 처리                             |
| ADM-010 | 가게 정보 관리        | 등록/수정/삭제, 제휴 관리                                      |
| ADM-011 | 관리자 대시보드       | 전체 통계 (종자 구매량, 로스율, 선호도) + 지자체 지표          |
| ADM-012 | 정책 데이터 관리      | 지자체 농업 지원 정책 DB 등록/갱신, 정책 매칭 룰 관리          |

---

## 4. 핵심 비즈니스 로직

### 4.1 수급 계산 공식 (🟦 수경)

```
공급 예측 = Σ(등록 면적 × 작물별 단위수확량) + 외부 공급 추정
수요 예측 = 지역 인구 × 1인당 소비량 × 계절 계수
수급 비율 = 공급 예측 / 수요 예측
```

### 4.2 수급 상태 5단계

| 상태       | 수급 비율 | 설명                |
| ---------- | :-------: | ------------------- |
| 🔴 과잉경고 |  > 150%   | 즉시 대안 작물 추천 |
| 🟠 과잉주의 | 120~150%  | 주의 알림           |
| 🟢 적정     |  80~120%  | 균형 상태           |
| 🟡 부족주의 |  50~80%   | 재배 권장           |
| 🔵 부족경고 |   < 50%   | 적극 재배 권장      |

### 4.3 AI 추천 점수 산정

```
추천 점수 = (수급 적합도 × 0.35) + (환경 적합도 × 0.25) + (수익성 × 0.25) + (농가 역량 × 0.15)
```

### 4.4 종자 등록 → 밸런스 반영 + 맞춤 분석 제공 흐름

```
농부: 종자 구매 등록 (씨앗/종자/모종, 수량, 영수증 인증)
  → 이벤트 발행 (동시 3가지 처리)
  │
  ├─ ① 수급 반영
  │     → 수급 동적 반영 엔진 (5분 이내 갱신)
  │     → 밸런스 차트 업데이트
  │     → 파종 밀도 120% 초과 시 쏠림 알림
  │     → AI 대안 작물 추천 트리거
  │
  ├─ ② AI 정책 매칭 (등록 유도 핵심 인센티브)
  │     → 등록한 작물 + 유저 조건 기반 지자체 정책 자동 탐색
  │     → "이 작물로 받을 수 있는 지원금: 최대 ○○만원" 즉시 표시
  │     → 신청 공문 생성 바로가기 제공
  │
  └─ ③ 예측 리포트 생성 (등록 유도 부가 인센티브)
        → 예상 수확량: "○○kg (면적 × 단위수확량 기준)"
        → 예상 수익: "○○만원 (현재 시세 × 예상 수확량)"
        → 수급 전망: "현재 이 작물은 [적정/과잉/부족] 상태"
        → 주변 농가 동향: "양평군 내 ○○명이 같은 작물 재배 중"
```

### 4.5 정책 매칭 · AI 공문 생성 흐름

```
진입 경로 (2가지):
  A) 종자 등록 완료 후 → 자동 트리거 (등록 직후 결과 카드로 표시)
  B) 정책 매칭 메뉴 직접 진입 → 작물 선택 후 수동 조회

  → AI 정책 분석 엔진
      ├── 지자체 농업 지원 정책 DB 탐색 (보조금, 자재 지원, 교육 등)
      ├── 유저 조건 매칭 (지역, 면적, 작물, 경력)
      └── 매칭 결과 (적용 가능 정책 목록 + 예상 수혜 금액)
  → AI 공문 초안 생성 (Gemini API)
      ├── 정책별 신청서 양식 자동 작성
      ├── 유저 정보 자동 반영 (이름, 농장, 면적 등)
      └── PDF 다운로드 / 수정 후 제출 안내
```

### 4.6 등록 유도 UX 전략

> 💡 **핵심 원칙**: 유저가 "등록하면 나에게 돌아오는 실질적 혜택"을 즉시 체감하게 만든다.

#### 등록 전 — 유도 메시지

```
종자 등록 화면 상단:
┌─────────────────────────────────────────────────────┐
│  🌾 심은 작물을 등록하면 이런 정보를 드려요!         │
│                                                     │
│  ✅ 내 작물로 받을 수 있는 지자체 보조금 정책 분석   │
│  ✅ AI가 신청 공문까지 자동 작성                     │
│  ✅ 예상 수확량·수익·수급 전망 리포트                │
│  ✅ 우리 지역 같은 작물 재배 현황                    │
└─────────────────────────────────────────────────────┘
```

#### 등록 후 — 즉시 결과 카드 (리워드 화면)

```
등록 완료 직후 결과 화면:
┌─────────────────────────────────────────────────────┐
│  🎉 등록 완료! 맞춤 분석 결과를 확인하세요                    │
│                                                     │
│  📋 매칭된 지원 정책: 3건                                │
│     └ 최대 수혜 금액: 350만원 [자세히 보기 →]              │
│                                                     │
│  📊 예측 리포트                                        │
│     └ 예상 수확량: 1,200kg                             │
│     └ 예상 수익: 480만원 (현재 시세 기준)                 │
│     └ 수급 상태: 🟢 적정 (안정적 재배 가능)                │
│     └ 양평군 내 동일 작물 재배: 47농가                    │
│                                                     │
│  [📄 공문 생성하기]  [📊 상세 리포트 보기]                  │
└─────────────────────────────────────────────────────┘
```

#### 미등록 유저 넛지

| 상황            | 넛지 메시지                                                                |
| --------------- | -------------------------------------------------------------------------- |
| 홈 화면 진입 시 | "아직 등록하지 않은 종자가 있나요? 등록하면 맞춤 보조금 정책을 찾아드려요" |
| 밸런스 조회 시  | "종자를 등록하면 내 농장 맞춤 수급 분석을 받을 수 있어요"                  |
| AI 추천 이용 시 | "추천 작물을 심으셨나요? 등록하면 지원 가능한 정책도 함께 알려드려요"      |

---

## 5. 통합 데이터 모델 (16개 엔티티)

```
User (유저)
├── id, email, password, name, phone, role, region, status, createdAt

Farm (농장)
├── id, userId(FK), name, address, areaSize, soilType
├── landCertImageUrl, landCertVerified, status(PENDING/APPROVED), createdAt

Crop (작물 마스터)
├── id, code, name, category, growthDays, yieldPerSqm, avgCostPerSqm
├── climateConditions(JSON), suitableFacility, isActive

SeedRegistration (종자 등록)
├── id, farmId(FK), cropId(FK), quantity, purchaseDate, sourceStore
├── receiptImageUrl, verified, createdAt

CropPlan (파종 계획)
├── id, farmId(FK), seedId(FK), cropName, plantingDate, targetYield, area

Harvest (수확 실적)
├── id, cropPlanId(FK), actualYield, harvestDate, qualityGrade

BalanceData (수급 데이터)
├── id, regionCode, cropId(FK), year, season
├── supplyForecast, demandForecast, supplyRatio, balanceStatus

Product (상품)
├── id, sellerId(FK), name, price, stock, description, imageUrl, status

Order (주문) / OrderItem (주문 항목)
CartItem (장바구니)

Post (게시글) / Comment (댓글)
├── 답변 채택(accepted) 필드 포함

Store (가게)
├── id, name, address, lat, lng, phone, category, partnerStatus

PolicyData (지자체 정책)
├── id, regionCode, policyName, category, targetCrops(JSON), conditions(JSON)
├── benefitAmount, applicationDeadline, sourceUrl, isActive, createdAt

PolicyMatch (정책 매칭 이력)
├── id, userId(FK), policyId(FK), cropId(FK), matchScore, estimatedBenefit
├── documentGenerated(Boolean), generatedDocUrl, createdAt
GuideMessage (권고 메시지)
Notification (알림)
```

---

## 6. 기술 스택

| 영역         | 기술                                            | 버전                       |
| ------------ | ----------------------------------------------- | -------------------------- |
| **Backend**  | Java + Spring Boot (헥사고날)                   | Java 21, Spring Boot 3.3.x |
| **ORM**      | Spring Data JPA (Hibernate)                     | —                          |
| **Build**    | Gradle - Groovy                                 | 8.10                       |
| **Frontend** | Next.js (App Router) + React + TypeScript       | Next 15, React 19, TS 5    |
| **Runtime**  | Node.js                                         | 22 LTS                     |
| **패키지**   | npm                                             | —                          |
| **CSS**      | CSS Modules (`.module.css`)                     | —                          |
| **DB**       | PostgreSQL (Alpine)                             | 16                         |
| **AI**       | Gemini 1.5 Flash + FastAPI + Python             | Python 3.12                |
| **지도**     | 카카오맵 API + Leaflet.js                       | —                          |
| **캐싱**     | Redis                                           | —                          |
| **보안**     | JWT (BFF httpOnly 쿠키) + BCrypt                | —                          |
| **인프라**   | Docker Compose v2                               | —                          |
| **CI/CD**    | GitHub Actions                                  | —                          |
| **API 표준** | `ApiResponse<T>`, CamelCase, `X-Correlation-Id` | —                          |

### 아키텍처 개요

```
[사용자 브라우저]
      ↓
[Next.js BFF (httpOnly 쿠키 ↔ JWT 변환)]
      ↓
[Spring Boot API Server (헥사고날)] ←→ [FastAPI (AI/Gemini)]
      ↓
[PostgreSQL] + [Redis (캐싱)]
      ↑
[공공 API: KAMIS, 기상청, 흙토람, 농넷]
```

### 보안 흐름 (BFF)

```
브라우저 → (httpOnly 쿠키) → Next.js BFF → (JWT Header) → Spring Boot API
```

### 에러 코드 체계

- 형식: `E-[역할]-[기능]-[번호]`
- 예: `E-FM-SEED-001` (농부-종자등록-첫번째 에러)

### API 응답 표준

```json
{
  "success": true,
  "data": { ... },
  "error": null,
  "meta": { "page": 1, "size": 20, "total": 100 }
}
```

---

## 7. 화면 구성 (역할별)

### 일반유저

| 화면             | 경로                  |
| ---------------- | --------------------- |
| 홈               | `/`                   |
| 회원가입         | `/auth/signup`        |
| 로그인           | `/auth/login`         |
| 마이페이지       | `/mypage`             |
| 커뮤니티         | `/community`          |
| 상점 (구매)      | `/shop`               |
| 상품 관리 (판매) | `/shop/seller`        |
| 주문 관리 (셀러) | `/shop/seller/orders` |
| 정책 매칭        | `/policy`             |
| 농장 등록 신청   | `/farm/register`      |

### 농부 (일반유저 + 추가)

| 화면                  | 경로            |
| --------------------- | --------------- |
| 농장 관리             | `/farm`         |
| 종자 등록             | `/farm/seed`    |
| 파종 계획             | `/farm/plan`    |
| 수확 실적             | `/farm/harvest` |
| 작물 밸런스           | `/balance`      |
| AI 추천               | `/recommend`    |
| 정책 매칭 / 공문 생성 | `/policy`       |
| 가게 정보             | `/stores`       |

### 관리자

| 화면             | 경로               |
| ---------------- | ------------------ |
| 관리자 대시보드  | `/admin`           |
| 유저 관리        | `/admin/users`     |
| 농부 승인        | `/admin/approvals` |
| RAG 데이터 관리  | `/admin/data`      |
| 커뮤니티 관리    | `/admin/community` |
| 상점 관리        | `/admin/shop`      |
| 가게 관리        | `/admin/stores`    |
| 정책 데이터 관리 | `/admin/policy`    |
| 시스템 헬스      | `/admin/health`    |

### 지자체

| 화면           | 경로               |
| -------------- | ------------------ |
| 수급 대시보드  | `/gov`             |
| 인접 지역 비교 | `/gov/compare`     |
| 재배 현황      | `/gov/cultivation` |
| 보고서         | `/gov/reports`     |
| 권고/공지 발송 | `/gov/notices`     |

---

## 8. 비기능 요구사항

| 항목          | 요구사항                                                       |
| ------------- | -------------------------------------------------------------- |
| **성능**      | API 응답 시간 500ms 이내, 수급 반영 5분 이내                   |
| **보안**      | JWT 만료 30분, Refresh 7일, 5회 실패 시 30분 잠금, BCrypt 해싱 |
| **접근성**    | 최소 글꼴 16px, 터치 영역 44px 이상 (고령 농업인 배려)         |
| **반응형**    | 모바일 퍼스트, 320px~1440px 대응                               |
| **캐싱**      | 밸런스 데이터 TTL 5분, 작물 마스터 TTL 24시간, 시세 TTL 1시간  |
| **로그 추적** | `X-Correlation-Id` 헤더로 요청 추적                            |

---

## 9. MVP 개발 일정 (5주)

### Phase 1 — MVP (P0)

| 주차  | 목표        | 핵심 기능                                                |
| :---: | ----------- | -------------------------------------------------------- |
|  1주  | 기반 구축   | DB 스키마, 회원가입/로그인, 농장 등록 API, 프로젝트 세팅 |
|  2주  | 핵심 데이터 | 종자 등록, 파종 계획, 작물 밸런스 API + 기본 차트 UI     |
|  3주  | AI + 관리   | AI 작물 추천, 커뮤니티 게시판, 관리자 기본 기능          |
|  4주  | 통합        | 지자체 대시보드, 역할별 접근 제어, 프론트 통합           |
|  5주  | 마감        | 통합 테스트, 버그 수정, 더미 데이터, 발표 준비           |

### Phase 2 — 확장 (P1)

- 직거래 상점 (판매/구매/장바구니/주문)
- 카카오맵 가게 정보
- 정책 매칭 시스템 (지자체 정책 DB + AI 공문 생성)
- 지자체 권고 발송, 수확 실적 등록

### Phase 3 — 고도화 (P2)

- 보고서 자동 생성 (PDF), 외부 API 실 연동, 정책 공문 고도화 (다양한 양식 템플릿)
- 밸런스 엔진 관리, 데이터 정합성 검증

---

## 10. 성공 지표 (KPI)

| 지표              | 목표 (6개월) |
| ----------------- | :----------: |
| 종자 등록 농가 수 |  ≥ 300 농가  |
| MAU               |    ≥ 500     |
| 추천 결과 열람률  |    ≥ 70%     |
| 수급 예측 정확도  |    ≥ 70%     |
| NPS               |     ≥ 40     |

---

> 💡 **다음 단계**: ① 16개 엔티티 ERD 확정 → ② API 엔드포인트 상세 설계 → ③ 와이어프레임 → ④ 프로젝트 셋업 (Spring Boot + Next.js + Docker)
