-- ═══════════════════════════════════════════════════════════════
-- V12: 농장(farms) 테이블에 반려 사유 컬럼 추가
-- 관리자가 농장 등록 신청을 반려할 때 사유를 저장합니다.
-- ═══════════════════════════════════════════════════════════════

ALTER TABLE farms ADD COLUMN IF NOT EXISTS reject_reason VARCHAR(500);
